"use client";

import { motion } from "framer-motion";

const Projects = () => {
  return (
    <section id="projects" className="py-32 bg-bg-paper min-h-screen">
      <div className="container mx-auto px-6">
        {/* Project content will be added here */}
      </div>
    </section>
  );
};

export default Projects;
